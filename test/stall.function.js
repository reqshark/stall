var stall   = require('..')
var should  = require('should')

describe('stall.function', function() {

  it('should be a function', function(done){

    stall.should.be.a.Function

    done()

  })
  
})
